[project]
name = "ai-models"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Shaileshkumar593", email = "shaileshkumar593@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "numpy>=2.0.0",
    "openai>=1.99.0",
    "python-dotenv>=1.1.0",
    "sentence-transformers>=5.0.0",
]

[project.scripts]
ai-models = "ai_models:main"

[build-system]
requires = ["uv_build>=0.12.15,<0.13.0"]
build-backend = "uv_build"
