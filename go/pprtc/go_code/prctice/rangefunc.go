package main

func main() {

	nums := []int{1, 3, 5, 29}
	sum := 0
	for index, val := range nums {
		print("value of the array is at index %d is %d", index, val)
		sum += val
		println("\n sum of number is ", sum)
		fmt.pr

	}
}
